# Weekly Progress Report

**Leader:**  

**Project Name:** Obscura

**Week #:** 4 | **Date Range:** 

---

## Individual Status Updates

| Team Member | Tasks Completed | Planned Tasks (Next Week) | Issues / Challenges | Time Spent |
|:------------|:---------------------------------------------------------------------------------------|:--------------------------------------------------------------------------------------------------------------------|:----------------------------------------------------| :--- |
| **Arjun** | - Conducted formal independent cross-reviews of ML Python implementations and OBS-13 ONNX export pipeline.<br>- Engineered detailed implementation blueprints for a new Proprietary Code Redaction model.<br>- Researched and defined explicit redaction criteria based on historical enterprise leaks (Apple, Samsung) and constrained strategies to Obscura's strict latency/RAM architecture.<br>- Extended PII detection engine to cover IPv6 addresses and international phone numbers (ITU-T E.164) with edge-case false-positive protections.<br>- Implemented Context-Aware Text Chunking into the NLP engine, resolving inference failures on long payloads by splitting at natural sentence boundaries with overlapping stride and global offset mapping.<br>- Reviewed and merged k6 baseline load testing framework (OBS-17). | - Begin development of the Proprietary Code Redaction ML pipeline (CodeBERT).<br>- Guide integration of CodeBERT ONNX inference into Rust Proxy. | Balancing strict <30ms latency budgets and <500MB RAM constraints while massively expanding ML pipeline capabilities. | 14-16 hrs |
| **Bigan** | - Implemented the OBS-16 midterm demo integration script (scripts/demo_obs16.py) to simulate end-to-end client requests through the Obscura proxy.<br><br>- Built a synthetic PII payload (name, phone, email, SSN, address) to test the proxy request pipeline and verify request forwarding.<br><br>- Implemented structured logging in the demo script to display request flow and proxy responses during testing.<br><br>- Generated the ONNX model artifacts (model.onnx, tokenizer files) using the export pipeline to allow the proxy to initialize locally.<br><br>- Successfully ran and validated the proxy locally with the demo script, confirming the request flow Client → Proxy → Response.<br><br>- Implemented a mock upstream fallback so the proxy can return a valid LLM-style response when no external API provider is configured, enabling the midterm demo to run locally without external API dependencies. | - Begin work on OBS-21: Implement SSE re-emission to client.<br><br>- Implement Server-Sent Events (SSE) response forwarding so that the proxy can stream responses to the client while preserving provider formatting.<br><br>- Ensure compatibility with the OpenAI Python SDK streaming mode.<br><br>- Test streaming responses through the proxy to confirm correct event forwarding and client consumption. | Initial proxy startup failed due to missing model artifacts and environment variables (NER_MODEL_PATH, NER_TOKENIZER_PATH). Generated ONNX artifacts locally and configured environment variables to allow the proxy to run. Additionally encountered upstream request failures when no API provider was configured, which required implementing a mock fallback to allow local testing. | 8-11 hrs |
| **Kyle** | Reviewed teammates PR's and assisted where possible. Improved local web demo that allows me to input text mixed with PII and see how the model preforms and what entities it labels. <br><br> Discovered struggling context length on messages that are particularly long leading to changes internally on how we batch process sections for the Bert Model. <br><br> Tested out per entity redaction and added it to the web demo allowing us to disable/enable certain NER-labels from being detected. | Working on getting the Mid Term demo working and deployed to our AWS infrastructure. <br><br> Continue expirementing with the detection model for increased accuracy and other important features. <br><br> Continue reviewing PR's and offering assistance where possible| N/A No major challenges encountered. | 8-12 hours|
| **Rainer** | - Integrated the ort crate <br> - Loaded the ONNX model <br> - re-wired the request pipeline <br> - Researched HIPPA guidelines (applicable in US) <br> - Began research on Texas guidelines as it pertains to health information and the sharing thereof (TLDR: we absolutely would be liable if information went through, which would be expensive) | - Write a report with considerations of HIPPA in four test states: Texas, California, Florida, and New York. See what all data has to be protected and what the pentalties for failing to protect this data are. <br> - These four states were chosen because they are the largest by population. They also have a nice split in that 2 of them are red and 2 of them are blue, and (likely more relevant for the sharing of information) 2 of them are one-party consent states, while two of them are all-party consent states. I promise this is a normal legal sentence. <br> - Additionally consider EU/UK health data privacy restrictions. | - API Volatility vs. Concurrency: The ort v2 ONNX RC forced us to wrap the inference engine in a blocking Mutex, creating an unavoidable bottleneck on Tokio's async routing throughput. <br> -Context Chunking Leaks: Overlapping 512-token chunks caused partial entities to override full entities; had to redesign the array sorting logic to explicitly sort by length descending to prevent PII loss. <br> - JSON Array Namespace Collisions: Multi-message prompts caused duplicate [PERSON_1] tokens to overwrite each other in the HashMap; built a global tracking middleware to namespace tokens across the entire request body. <br> - Schema Security Risk: The proxy silently failed-open on unrecognized, non-chat payloads (like legacy {"prompt"} schemas); had to implement a strict fail-closed fallback to block unredacted leaks. <br> - No lawyer in the history of ever has written things normally. | 10 hrs|
| **Eduardo** | | | | |
| **Billy**| Completed the local portions of OBS-18 by reviewing Docker logs after a synthetic end-to-end test run and checking for evidence of raw synthetic PII leakage. <br><br> Verified local environment-file handling by checking whether `.env`-style files appeared in tracked files and reviewing the repository `.gitignore`. <br><br> Ran a repo-wide keyword scan for common hardcoded secret indicators, excluding generated directories, and documented that no obvious hardcoded secrets were found in the reviewed project files. <br><br> Drafted the OBS-18 audit report and left the EC2 security group review section pending direct AWS access or teammate-provided screenshots. | Implement Tower middleware to validate the `Authorization: Bearer` header against an environment variable. <br><br> Return `401 Unauthorized` when the header is missing and `403 Forbidden` when the provided key is invalid. <br><br> Add constant-time comparison for API key validation and test the middleware locally against the existing proxy flow. | EC2/security group details were not available locally, so the cloud deployment review section remains pending until access or screenshots are provided. | 8-10 hours |

**Total Team Time Spent:** 14-16 hrs

---

## Weekly Summary
