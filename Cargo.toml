[workspace]
members = [
    "config",
    "inference",
    "proxy",
]
resolver = "2"

# Centralized dependencies for workspace-wide consistency
[workspace.dependencies]
tokio = { version = "1.37", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
